/**
 * Created so that internal tsconfig.json files
 * can reference "@kwin-ts/types" in "types"
 * the same way that external projects can,
 * when they install the published @kwin-ts/types
 * package.
 */

/// <reference types="../../../packages/types" />
